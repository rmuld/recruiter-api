spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/candidates
spring.datasource.username=root
spring.datasource.password=tere

app.jwtSecret=SuurSaladus

# FILE UPLOAD
# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
# Max file size.
spring.servlet.multipart.max-file-size=200MB
# Max Request Size
spring.servlet.multipart.max-request-size=200MB

# Uploaded files are stored in the following directory.
file.upload-dir=C:/Users/opilane/Desktop/meie-projekt/uploads
